/**
 * "mr_stack.h"，堆栈数据结构处理函数
 *
 * mr_stack库提供一种基于数组构造的线性堆栈，用于实现先进后出的存取元素。
 * 压入新元素时如超出当前堆栈容量，Stack会自动扩展容量，但弹出栈顶元素时不会自动释放空余的空间
 * Stack可以保存重复的元素，但不接受NULL
 *
 * Version 0.0.1, 李斌，2016/02/28
 */
#ifndef MR_STACK_H
#define MR_STACK_H

#include "mr_common.h"

#endif
